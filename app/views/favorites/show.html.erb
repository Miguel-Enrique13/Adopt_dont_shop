
<% session[:favorites].each do |pet| %>
<div class="card">
  <div class="card-title">
    <%= link_to "#{pet["name"]}", "/pets/#{pet[:id]}" %>
    <div class="card-body">
      <%= image_tag asset_path(pet["image"]), class: "card-img-top" %>
    </div>
  </div>
</div>
<% end %>
