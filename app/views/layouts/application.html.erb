<!DOCTYPE html>
<html>
<head>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">

  <title>AdoptDontShop</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>


  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>

<body>
  <nav class="navbar sticky-top navbar-light bg-light" id="NavBar">
    <form class="form-inline">
      <a class="btn btn-outline-primary" type="button" href="/shelters"> Shelters </a>
      <a class="btn btn-outline-dark" type="button" href="/pets"> Pets </a>
      <a class="btn btn-outline-warning" type="button" href="/favorites"> Favorites: <%= session[:favorites].count if session[:favorites] != nil  %> </a>
    </form>
  </nav>


  <% flash.each do |type, message|  %>
    <% if type == "red"%>
      <div class="alert alert-danger">
        <%= message %>
      </div>
    <% elsif type == "yellow"  %>
      <div class="alert alert-warning">
        <%= message %>
      </div>
    <% elsif type == "blue"  %>
      <div class="alert alert-primary">
        <%= message %>
      </div>
    <% end %>
  <% end %>


  <% if (controller_name == "shelters" || controller_name == "pets") && (action_name == "new" || action_name == "edit") %>
  <div class="card">
    <div class="card-body">
      <%= yield %>
    </div>
  </div>
  <% else %>
  <%= yield %>
  <% end %>
</body>
</html>
